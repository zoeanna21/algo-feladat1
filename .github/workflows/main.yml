name: CC Autograde

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    - name: Build with Maven
      run: mvn clean test
      env:
        AID: "5710353417633792"
        CID: "2f0ad952-5c33-4c7b-a7da-9951df5b6f42"
                                    
